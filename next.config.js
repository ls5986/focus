/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/focus',
  assetPrefix: '/focus/',
}

module.exports = nextConfig 